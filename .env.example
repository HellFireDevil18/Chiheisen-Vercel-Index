# PostgreSQL Database URL
# Get this from your PostgreSQL provider (Vercel Postgres, Neon, Railway, etc.)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Optional: Key prefix for token storage (useful for multiple deployments)
KV_PREFIX=

# Microsoft OAuth Configuration (set these in your Vercel environment variables)
# NEXT_PUBLIC_USER_PRINCIPLE_NAME=your-email@example.com
# CLIENT_ID=your-microsoft-app-client-id
# CLIENT_SECRET=your-microsoft-app-client-secret
# REDIRECT_URI=https://your-domain.vercel.app/onedrive-vercel-index-oauth/step-3